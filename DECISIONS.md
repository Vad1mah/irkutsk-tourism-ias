# Лог решений

Формат: что решили → почему → что теперь запрещено.
Пишется по ходу работы, читается в начале сессии наравне с CLAUDE.md.

---

## 2026-08-10 — Пер-ивентные цифры влияния убраны из продукта, регрессия выведена в UI

**Решение пользователя.** Из UI и из тулов агента убраны любые цифры влияния
отдельного события; события остаются календарём. Результат регрессии при этом
выведен в продукт, а не оставлен скриптом.

**Что это значит в коде.** Удалены `/api/analytics/events-impact` с тремя
хелперами, `MethodologyService.compute_seasonal_baseline` и `corrected_impact`,
`scripts/check_impact_ci_calibration.py`, блоки impact в `Analytics.tsx`,
`Events.tsx`, `Home.tsx`. Добавлен `GET /api/analytics/events-effect` и блок на
вкладке «События». Тул агента `get_top_events_by_impact` заменён на
`get_event_effect` — число тулов прежнее, 12.

**Почему удалением, а не правкой подписей.** В блоке было три расхождения кода и
текста сразу («медиана» при `mean`, «процентные пункты» при относительном
изменении, метка «Достоверность» из одного `n_samples`). Чинить подписи у таблицы,
чьи числа измерение всё равно не подтвердило, — работа впустую.

**Запрещено теперь.** Возвращать в UI или в тулы агента прирост загрузки,
приписанный отдельному событию. Показывать оценку по району, где число
независимых эпизодов ниже `MIN_EVENT_EPISODES`.

---

## 2026-08-10 — Прогноз в агенте: только по прямой просьбе и только с дисклеймером

**Решение.** `forecast_occupancy` вызывается лишь когда пользователь прямо просит
прогноз, и первой строкой ответа инструмент возвращает оговорку о бэктесте.

**Почему.** Проверено на проде: на вопрос про события агент по своей инициативе
дёрнул `forecast_occupancy` (`sources: ['search_events','get_top_events_by_impact',
'forecast_occupancy']`). При запрете предъявлять прогноз это означало, что на
показе агент сам назовёт число, которое система же и опровергла бэктестом.

**Запрещено теперь.** Убирать дисклеймер из возвращаемого текста инструмента —
он адресован модели и обязан дойти до пользователя.

---

## 2026-08-10 — Материалы показа лежат в docs/demo/, числа берутся заново перед встречей

**Решение.** `docs/demo/`: `scenario-12min.md`, `objections.md`,
`failure-playbook.md`, `one-pager-template.md`. Числа в них сняты с прода
11.08.2026 и сверены с живыми ответами эндпоинтов.

**Почему это важно зафиксировать.** Данные обновляются ежедневно, и через неделю
таблицы в сценарии устареют. В одностраничнике район подставляется **URL-кодированным**:
кириллица в адресе из Git Bash уходит искажёнными байтами и все запросы отвечают 400.

**Запрещено теперь.** Показывать материалы, не обновив числа в день встречи.

---

## 2026-08-10 — Событийный эффект измерен регрессией: в Иркутском районе его нет

**Что построено.** `MethodologyService.event_effect_regression` +
`backend/scripts/event_effect_regression.py`. Модель:
`occupancy[район, день] = константа[район × календарный месяц] + день_недели + tau * событие`.
Сезонность снимается непараметрически помесячными константами — параметра
ширины окна в методе нет вовсе, подкручивать нечего.

**Результат на прод-данных** (1137 наблюдений, 3 района, 2025-03-20 — 2026-08-10):

| выборка | tau, п.п. | 95% CI | плацебо | эпизодов | событ. дней |
|---|---|---|---|---|---|
| все районы | +4.63 | [-0.69, +9.95] | 0.0074 | 49 | 151 |
| Иркутский | +0.80 | [-1.84, +3.43] | 0.6158 | 43 | 119 |
| Ольхонский | +19.57 | [+13.09, +26.05] | 0.0004 | 2 | 28 |
| Слюдянский | +3.17 | [-3.89, +10.24] | 0.4099 | 4 | 4 |

**Вывод: в Иркутском районе событийного эффекта на загрузку не обнаружено.**
Это единственный район, где события разложены по всему году (43 независимых
эпизода, 27 из них — одиночные дни), то есть где сравнение вообще возможно.

**Ольхонские +19.57 предъявлять нельзя.** В районе за всю историю два события,
оба — `Olkhon Ice Fest` 15.02–15.03, и в окно данных попадает ровно один
фестиваль 2026 года. Он поставлен на пик ледового сезона намеренно, поэтому
внутримесячное сравнение всё равно упирается в «середина февраля — середина
марта против начала февраля и конца марта», то есть в пик льда против его
склонов. Разделить фестиваль и лёд на этих данных невозможно ни одной
поправкой. По той же причине круговой плацебо-сдвиг для него не является
корректной нулевой гипотезой: у намеренно приуроченного события даты не
обмениваемы.

**Три ошибки спецификации, найденные и устранённые по ходу** (каждая
переворачивала число, поэтому важны):

1. Общая на регион сезонная кривая. Гармоники дня года давали +6.02 п.п.
   пулированно, при этом оценка ползла с числом гармоник (+6.4 при одной паре
   против +1.6 при шести) — подпись протекающей сезонности. Заменено на
   помесячные константы: тюнингового параметра не осталось.
2. Кластеризация ошибки по датам. Месячный фестиваль считался 28 независимыми
   наблюдениями; SE 0.78 против 2.64 после переноса кластера на ячейку
   сравнения. Именно это переводило пулированную оценку из «значима» в «нет».
3. Поденная перестановка в плацебо-тесте повторяла ту же ошибку (p=0.0002).
   Заменена на круговой сдвиг календаря событий целыми неделями — сохраняет и
   длины серий, и привязку к дню недели. Сдвиг идёт по календарю, а не по
   номерам строк: провал данных 24.06–25.10.2025 иначе ломает обе привязки.

**Калибровка процедуры** (`tests/test_event_effect_regression.py`, 10 passed):
на 40 синтетических выборках без эффекта интервал промахивается в 7.5% случаев
при номинале 5%, средний сдвиг оценки 0.02 п.п.; плацебо даёт p<0.05 в 5%
случаев; заложенные +8.0 п.п. восстанавливаются как +7.71 [6.69, 8.73].
Отдельный тест воспроизводит ольхонскую конфигурацию (события на собственном
сезонном пике района) и требует, чтобы она не давала ложного эффекта.

**Запрещено теперь.** Предъявлять ольхонские +19.57 как эффект фестиваля.
Кластеризовать ошибку по отдельным датам при многодневных событиях.
Использовать поденную перестановку как плацебо. Возвращать параметрическую
сезонность из гармоник в этот расчёт.

---

## 2026-08-10 — Окно baseline: вариант «расширить до ±8 недель» отвергнут измерением

**Измерено на проде** (`/api/analytics/events-impact`, окна 3/4/6/8/12/16/26,
скрипты разбора — `.tmp_probe/`, прогнаны 10.08).

Расширение окна **не сокращает интервал**: медиана ширины CI 49.5 при w=3 и
55–67 при w=8..26, хотя медианное n растёт с 3 до 24. Причина структурная —
в `t*std*sqrt(1+1/n)` множитель `sqrt(1+1/n)` стремится к 1, то есть ширина
упирается в собственный разброс одного дня, а не в погрешность среднего.
Число «значимых» скачет 7 → 0 → 2 → 10 → 30 → 1 и сидит на 1–9 уникальных датах.

Контроль на **фиксированном** наборе 27 пар (дата, район), присутствующих во всех
семи окнах: загрузка в день события неизменна (62.4), а `baseline_mean` падает
61.1 → 55.1 (w=3 → w=12) и разворачивается на 56.8/56.9 (w=16/26). Средняя
относительная дельта растёт +1.9 → +13.1 → +9.5 исключительно за счёт этого падения.

**Проверка через `adversarial-verify` (5 скептиков + арбитр): 5/5 «опровергнуто».**
Что устояло и что нет:

- Устояло: отказ от ±8 недель. Ни один скептик не показал, что широкое окно даёт
  годную оценку.
- Опровергнут заявленный **механизм**. «Чистое сезонное смещение» дало бы гладкий
  тренд, а `baseline_mean` немонотонен (минимум на w=12). Правдоподобнее — артефакт
  провала данных 24.06–25.10.2025 и смены состава доступных дней. Механизм считать
  недоказанным.
- Опровергнута **подача цифры** «+1.9 как честный вывод»: пулированная формула
  `s/sqrt(K)` не калибрована (калибровался per-event интервал предсказания),
  а наблюдения не независимы (соседние события делят baseline-дни, события одной
  даты в разных районах делят дневной шок).

**Два возражения скептиков на данных не подтвердились** (проверено, а не принято
на веру): доля fallback-категории `event` в пуле — 48%, а не 78%; выброс при
переходе к пересечению окон — 0 из 27, а не 65%. Исключение fallback-категории
вывод не переворачивает: w=3 даёт +3.1 [-0.8, +7.0] (K=14), w=8 даёт
+9.1 [+4.3, +14.0] (K=24) — зависимость от окна остаётся и без «спама».

**Запрещено теперь.** Ставить `window_weeks` больше 3 «чтобы интервалы сузились» —
измерено, что они не сужаются. Предъявлять пулированную оценку с интервалом
`s/sqrt(K)` как измеренную, пока не сделан кластерно-робастный SE. Ссылаться на
«сезонное смещение» как на установленную причину.

---

## 2026-08-10 — Три расхождения кода и подписей в блоке событийного impact

**Найдено при разборе.** Блок «Топ-10 событий по влиянию на спрос»
(`frontend/src/pages/Analytics.tsx`) — предполагаемый центр показа отельеру —
описан неверно в трёх местах:

1. Подзаголовок карточки и `MethodologyTooltip` говорят «**медианы** по похожим
   дням недели». `MethodologyService.compute_seasonal_baseline` считает
   `statistics.mean`, а не медиану.
2. Тот же тултип говорит «Отклонение в **процентных пунктах**». Считается
   относительное изменение `(observed - baseline)/baseline*100`; колонка подписана
   «Δ отн.» и рендерит `%` — то есть тултип противоречит и коду, и соседней колонке.
   На базе ~60% «+8.7%» — это ~+5 п.п., разница существенная.
3. Колонка «Достоверность» показывает high/medium/low, выведенные **только из
   `n_samples`**. Эмпирически метка не связана с точностью: при w=3 у `high`
   медианная ширина интервала 118.8, у `medium` — 20.1. То есть «высокая
   достоверность» стоит рядом с самыми широкими интервалами.

**Запрещено теперь.** Показывать этот блок отельеру до устранения расхождений:
профессионал с rate shopper'ом проверит «медиану» и «п.п.» первым же вопросом.

---

## 2026-08-10 — RAG поднят: дело было не в ключе, а в том, что переиндексация не запускалась

**Что сделано.** На проде вызван `POST /api/documents/reindex` — ответ
`{"indexed_count":489,"total_documents":489,"collection_count":489,"errors":[]}`,
`/health` показывает `chroma_docs: 489` вместо 0. Проверка чата
(`POST /api/query`, 200 за 50.8 с) — агент отвечает по корпусу, зовёт
`search_events`, `get_top_events_by_impact`, `forecast_occupancy`.

**Почему было 0.** Ключ GigaChat рабочий: прямой вызов `GigaChatEmbeddings.embed_documents`
в прод-контейнере вернул `EMBED OK dim=1024`. Ноль держался потому, что суточный
cron переиндексации стоит на 05:30, а контейнер до 17:00 UTC крашился на старте
(`OSError: [Errno 28] No space left on device: '.cache'`) — до своего cron он ни разу
не дожил. Переход на локальные эмбеддинги, записанный решением от 09.08, пока не нужен.

**Запрещено теперь.** Считать `chroma_docs: 0` доказательством отказа эмбеддера,
не проверив отдельно (а) сам вызов `embed_documents` и (б) что контейнер прожил
до времени cron'а.

---

## 2026-08-11 — Индекс RAG стирался каждым деплоем: путь Chroma мимо тома

**Что найдено.** В `docker-compose.yml` том смонтирован в `/app/chroma_data`, а
`settings.chroma_persist_dir` по умолчанию `./chroma_db` — то есть Chroma писала
внутрь контейнера. Том оставался пустым (`ls /app/chroma_data` — ничего), а
`docker compose up -d --force-recreate` уносил индекс вместе со слоем контейнера.

Проверено прямо: после сегодняшнего деплоя `chroma_docs` снова упал в 0 при том,
что днём он был 489, а ключ GigaChat рабочий.

**Решение.** `CHROMA_PERSIST_DIR=/app/chroma_data` задан явно в блоке
`environment` сервиса бэкенда. Дефолт в `config.py` не трогаем: `./chroma_db`
корректен для локального запуска вне контейнера.

**Почему это важнее, чем кажется.** Каждое падение `chroma_docs` в 0 выглядело
как отказ эмбеддера и дважды уводило диагностику в сторону ключа и квоты.
Настоящая причина была в путях.

**Запрещено теперь.** Считать `chroma_docs: 0` после деплоя нормой. Менять точку
монтирования тома, не меняя `CHROMA_PERSIST_DIR` — и наоборот.

---

## 2026-08-11 — Фикс парсеров подтверждён на проде: 6 источников из 7 в ok

**Замер до правки:** `kassir`, `culture_rf`, `yandex_afisha`, `telegram` —
`fail` с `timeout 120s`, собрано 0. Работали только `irk`, `culture38`, `zeroevent`.

**Замер после правки** (`POST /api/parser/events/all`, 123.3 с, 61 событие):

| Парсер | Статус | Собрано |
|---|---|---|
| irk | ok | 27 |
| kassir | ok | 23 |
| culture38 | ok | 2 |
| culture_rf | ok | 0 |
| yandex_afisha | ok | 0 |
| zeroevent | ok | 0 |
| telegram | fail | 0 |

Отдельный прогон `kassir` — 23 события за 18.2 с против прежнего таймаута в 120 с.

**Что подтвердилось.** Причиной массового отказа был сон под общим локом
rate-limiter'а: четыре Jina-зависимых парсера выстраивались в очередь и выбирали
таймаут, ни разу не сходив в сеть. После выноса сна из-под лока и закорачивания
451 источники, у которых есть рабочий путь, отвечают за секунды, а у остальных
статус стал честным (`ok` с нулём вместо `fail` с таймаутом).

`telegram` остаётся в отказе — t.me недостижим с сервера на сетевом уровне,
чинить не планировалось.

---

## 2026-08-10 — culture_rf не чинится: источник не отдаёт дату начала события

**Что сделано.** Путь загрузки переведён с Jina на встроенное состояние Next.js
(`__NEXT_DATA__ → props.pageProps.events.items`) — обычный GET, без браузера.
Извлечение работает и разбирает все 20 записей листинга.

**Почему событий всё равно ноль.** В листинге нет даты начала: `date` и
`seanceStartDate` пусты у всех 20 записей, заполнен только `seanceEndDate`.
Детальные страницы по ссылкам из **собственной** microdata culture.ru отвечают
404, поле `places[].schedule` пусто у всех записей. Парсер отбрасывает записи без
даты начала и логирует причину.

**Почему не подставить дату окончания.** Даты событий теперь питают оценку
событийного эффекта. Сдвинутая на недели дата испортит оценку молча и
необнаружимо — это хуже, чем ноль событий из источника.

**Запрещено теперь.** Брать `seanceEndDate` как `date_start`. Считать culture_rf
починенным: путь загрузки исправен, ограничение на стороне источника.

---

## 2026-08-10 — Парсеры kassir/culture_rf/yandex/telegram: причина не Brotli, а 451 от Jina

**Что измерено на проде.** Все четыре парсера падают с `timeout 120s`. Разобрано по шагам:

1. `crawl4ai`, `playwright`, `lxml` в прод-образе **отсутствуют** (проверено
   `importlib` внутри контейнера) — значит `CRAWL4AI_AVAILABLE=False` и весь AI-путь
   сразу уходит в Jina.
2. `https://r.jina.ai/...` с прод-IP отдаёт **HTTP 451 Unavailable For Legal Reasons**
   (0.1 с). При этом прямой HTTPS к самим источникам с того же хоста работает:
   `irk.kassir.ru` 200 / 1.09 МБ, `culture.ru` 200, `afisha.yandex.ru` 200.
3. `AdaptiveRateLimiter.get_delay()` = `5 + 10*errors_count`, потолок 60 с. Каждый 451
   инкрементит `errors_count`, поэтому задержка перед следующим вызовом растёт до 60 с.
4. `AdaptiveRateLimiter.wait_for` делает `await asyncio.sleep(...)` **внутри
   `async with self._lock`**, а lock один на все домены. Четыре Jina-зависимых парсера
   в общем `asyncio.gather` выстраиваются в очередь на этом локе и выбирают
   `asyncio.wait_for(..., timeout=120)` целиком.

Отсюда детерминированная картина: `irk` (26), `culture38` (2), `zeroevent` (0) — успех,
все Jina-зависимые — `fail` ровно через 120 с после старта прогона.

**Запрещено теперь.** Записывать эти четыре парсера в «чинится установкой Brotli» —
Brotli 1.2.0 в образе стоит и к отказу отношения не имеет. Держать `asyncio.sleep`
под общим локом rate-limiter'а. Чинить Jina-путь заменой заголовков: 451 выдаётся
по географии IP, а не по User-Agent.

---

## 2026-08-09 — Демо для отельера строится вокруг бенчмарка, а не прогноза

**Решение.** Центр показа реальному отельеру — бенчмарк сегмента (загрузка / ADR-прокси /
RevPAR-прокси района и сегмента район×тип×размер, перцентили цен, сравнение районов)
плюс событийный impact в п.п. с CI. Прогноз показывается только на горизонте 1–2 дня
под подписью «оперативный».

**Почему.** Измерено на проде: сохранённый ансамблевый прогноз от 28.07 на окне
29.07–09.08 дал MAE 19.2 п.п., наивный «завтра как вчера» на том же окне — MAE 2.75.
Модель предсказала спад 65 % → 37.5 %, факт держался 64–73 %. Причина установлена:
провал данных 24.06–25.10.2025 (122 дня) оставил Prophet без наблюдений высокого
сезона, годовая компонента в июле–августе не закреплена. Свежий walk-forward CV:
xgboost 5.16 ± 2.67, ensemble 6.32 ± 7.03, prophet 12.01 ± 14.30 — ансамбль хуже
одиночного XGBoost.

**Запрещено теперь.** Показывать отельеру горизонты 7+ дней, доверительные интервалы
(не калиброваны: ci_coverage 0.11 в одном запуске и 1.0 при ширине 72 п.п. в другом),
любые R² (ряд почти плоский, σ ≈ 4.7 п.п. — метрика бессмысленна), формулировку
«ансамбль из трёх моделей» (NeuralProphet вернул 0 точек в 96 вызовах из 96).

---

## 2026-08-09 — Прогноз убирается из демо целиком (пересмотр решения выше)

**Что изменилось.** `adversarial-verify` (3/3 «опровергнуто», high confidence) забраковал
формулировку «прогноз на 1–2 дня с ошибкой ~2 п.п.»: число бралось из таблицы `forecasts`,
где upsert затирает прогнозы разных горизонтов, смешивало докризисный июнь со сломанным
июлем и покрывало один район. Проведён контролируемый rolling-origin бэктест
(`backend/scripts/backtest_forecast.py`, 31 origin, 29.03–02.08.2026, горизонт 7,
парный бутстрэп против наивного «завтра как вчера»).

**Результат (Иркутский, весь период):**

| h | MAE модели | MAE наив | p(наив не хуже) |
|---|---|---|---|
| 1 | 2.83 | 2.82 | 0.509 |
| 2 | 3.39 | 3.96 | 0.134 |
| 3 | 3.73 | 4.69 | 0.069 |
| 6 | 4.96 | 4.59 | 0.701 |
| 7 | 4.75 | 3.81 | 0.914 |

**Текущий сезонный режим (7 origins, 03.07–02.08): модель проигрывает наивному
на ВСЕХ горизонтах 1–7**, систематическое занижение (bias −3.3 … −6.7 п.п.).

**Важная оговорка о самом замере.** Локально Prophet сломан
(`'Prophet' object has no attribute 'stan_backend'`), поэтому ансамбль в бэктесте
состоял только из XGBoost — то есть измерялся лучший компонент по прод-CV
(5.16 против 6.32 у ансамбля). Замер сделан в пользу системы, и она всё равно
не обыгрывает тривиальный базлайн.

**Решение.** Прогноз не предъявляется отельеру как работающая возможность ни на одном
горизонте. Вместо этого предъявляется сам факт измерения: кривая ошибки против наивного
базлайна, причина (провал данных за высокий сезон) и то, что нужно для починки.
Это переводит слабое место в аргумент о добросовестности и даёт конкретный предмет пилота.

**Запрещено теперь.** Называть цифру «~2 п.п.» и любые числа из
`/api/forecast/{district}/validation` — эта панель читает затираемую таблицу.
Источник истины по точности — только `backend/scripts/backtest_forecast.py`.

---

## 2026-08-11 — Тестирование на реальных данных: SSH-туннель к прод-postgres, не локальный Docker

**Решение.** Полный `pytest` гоняется против **копии прод-БД** через SSH-туннель:
`ssh -f -N -L 15432:127.0.0.1:5433 user1@82.202.143.11` плюс отдельная база
`tourism_test` на сервере (создаётся `pg_dump tourism | psql tourism_test`).
Пароль берётся из `docker exec tourism-postgres printenv POSTGRES_PASSWORD`
и передаётся через stdin — на диск не пишется и в транскрипт не попадает.

**Почему.** Локального PostgreSQL нет, Docker Desktop на этой машине сильно
тормозит. Без БД полный набор обрывался на подключении и терял сводку, из-за чего
DB-зависимые тесты не выполнялись **никогда** — так протух `test_correlation_gap_filter`,
сравнивавший с литералом 5 вместо `MIN_SAMPLES_PER_MONTH`. С туннелем: 228 passed.
Отдельная база нужна, чтобы пишущие тесты не трогали прод.

**Запрещено теперь.** Гонять тесты напрямую по базе `tourism`. Запускать `pytest`
без `--junitxml`: процесс падает при завершении интерпретатора и сводка теряется.

---

## 2026-08-11 — Деплой обязан начинаться с проверки диска

**Что произошло.** `docker compose pull` подтянул новый образ бэкенда (~4.8 GB),
старый остался висячим — диск ушёл в 100%, бэкенд свалился в crash-loop с
`OSError: [Errno 28] No space left on device` на создании `.cache`. Лечение:
`docker image prune -f` (2.59 GB) + `docker builder prune -f` (2.92 GB), диск 72%.

**Почему это повторяемо.** Образ бэкенда тяжёлый (torch), а VM имеет 30 GB. Каждый
деплой временно требует места под две версии сразу.

**Запрещено теперь.** Запускать `docker compose pull` на этой VM, не посмотрев
`df -h /`. Ниже ~8 GB свободного — сначала `docker image prune -f`.
Отдельно: после `docker compose up -d --force-recreate` обязателен
`docker restart proxy-nginx`, иначе nginx держит старый IP контейнера и отдаёт 502.

---

## 2026-08-11 — libgomp1 ставится в Dockerfile явно

**Решение.** `libgomp1` добавлена в список пакетов `apt-get install` в
`backend/Dockerfile`, хотя её же тянет за собой `gcc`.

**Почему.** Ниже по файлу идёт `apt-get purge -y --auto-remove gcc g++`, и apt
считает `libgomp1` осиротевшей зависимостью компилятора. После пересборки образа
она исчезла, `import lightgbm` начал падать с `libgomp.so.1: cannot open shared
object file`, а `/api/forecast/ensemble` — отдавать 500 на проде.

**Запрещено теперь.** Убирать `libgomp1` из явного списка как «дубль зависимости gcc».

---

## 2026-08-11 — ОТКРЫТЫЙ ВОПРОС: пер-ивентный impact не выдерживает честной статистики

**Что обнаружено после деплоя исправленного интервала.** На проде из 78 событий
с посчитанным CI значимы (интервал не накрывает ноль) только 7 — и у всех семи
одинаковые `delta_pct = 8.74`, `CI = [1.96, 15.53]`, то есть это одна и та же дата,
а не семь независимых наблюдений. Распределение размера базовой выборки:
n=2 → 29 событий, n=3 → 37, n=4 → 10, n=5 → 2.

**Причина — не формула, а окно baseline.** `window_weeks=3` с совпадением дня недели
даёт 2–5 опорных дней. На такой выборке честный интервал широким будет всегда;
прежняя (заниженная) формула это просто скрывала.

**Следствие для демо.** Утверждение «событие X дало +N п.п.» на текущих данных
предъявлять нельзя. Каркас показа, одобренный adversarial-verify, в этой части
требует пересмотра.

**Варианты, ни один пока не выбран (решение за пользователем — это пороговый параметр):**
1. Расширить окно baseline до ±8 недель: даст ~16 опорных дней, интервалы сузятся,
   ценой сезонной локальности. Параметр уже есть: `window_weeks`, допускает 1..52.
2. Пулить события по типу/размеру и показывать средний эффект категории,
   а не отдельного события.
3. Отказаться от заявлений о значимости и показывать только «даты выше сезонной
   нормы» с явной пометкой размера выборки.

---

## 2026-08-10 — NeuralProphet удалён из ансамбля и из всех пользовательских текстов

**Решение.** `ensemble_service` больше не вызывает `neuralprophet_service`; веса по
умолчанию стали `prophet 0.5 / xgboost 0.5`. Из UI (`About`, `Forecast`, `localize`)
и из промптов/докстрок бэкенда убраны упоминания «ансамбль из трёх моделей».
Сам `neuralprophet_service.py` оставлен в репозитории, но в пайплайне не участвует.

**Почему.** Две причины, и вторая важнее первой.
1. Он не работает: «извлечено 0 точек прогноза» — 96 вызовов из 96 в логах прода.
   В таблице метрик строка просто исчезала, а тексты продолжали обещать три модели.
2. Он обучается на каждом запросе («Обучение NeuralProphet на 494 точках») и как
   torch-модель является главным потребителем памяти. На VM 3.8 GiB без swap
   параллельные обращения к `/api/forecast/*` уже привели к отказу всего хоста.

Выход ансамбля численно не меняется — нулевые точки и так не попадали в результат.

**Запрещено теперь.** Возвращать NeuralProphet в ансамбль без (а) доказанного
ненулевого извлечения прогноза и (б) выноса обучения из request-path. Писать
«ансамбль из трёх моделей» где-либо в UI и промптах.

---

## 2026-08-10 — Интервал событийного impact пересчитан: t-квантиль и sqrt(1 + 1/n)

**Решение.** В `MethodologyService.corrected_impact` полуширина интервала считается как
`t(0.975, n-1) * std * sqrt(1 + 1/n)` вместо `1.96 * std / sqrt(n)`.

**Почему.** Интервал строится вокруг разности «одно наблюдение минус среднее по похожим
дням». Прежняя формула учитывала только погрешность среднего и игнорировала собственный
разброс единичного наблюдения, занижая ширину в sqrt(n+1) раз; множитель 1.96 при
типичных n=2..6 добавлял ещё одно занижение.

Измерено, а не выведено: `scripts/check_impact_ci_calibration.py` прогнал обе формулы
по 266 безсобытийным дням Иркутского (у них истинный impact = 0, значит ноль обязан
попадать внутрь 95%-интервала примерно в 95% случаев).

| формула | фактическое покрытие |
|---|---|
| прежняя `1.96*s/sqrt(n)` | **63.5%** (при n=2 — 46.2%) |
| текущая `t*s*sqrt(1+1/n)` | **98.9%** |

То есть прежний интервал помечал как значимое отклонение больше трети обычных дней.
Это критично: событийный impact — центр демо для отельера.

**Запрещено теперь.** Возвращать нормальный квантиль и формулу без `sqrt(1 + 1/n)`.
Менять формулу интервала, не прогнав `check_impact_ci_calibration.py` и не показав покрытие.
`scipy` объявлена в `requirements.txt` явно — она теперь прямая зависимость, а не транзитивная.

---

## 2026-08-09 — Горизонт прогноза ограничен 14 днями на уровне API и UI

**Решение.** `MAX_FORECAST_HORIZON_DAYS = 14` в `backend/app/constants.py`; им заменены
`le=365` в `/api/forecast/ensemble` и `/api/forecast/explain`. В UI выбор горизонта
сокращён с 7/14/30/60/90 до 3/7/14 с подписями о качестве.

**Почему.** Две причины, обе измеренные. Первая: за 14 днями ошибка модели превышает
ошибку наивного «завтра как вчера», то есть длинный прогноз не несёт информации.
Вторая, операционная: точки длинного прогноза складываются в ту же таблицу `forecasts`,
где действует `UniqueConstraint(district, forecast_date, model)` — выбор «90 дней»
затирал ближние даты, и панель самовалидации потом показывала ошибку 90-дневного
прогноза под подписью «за 14 дней». Мина срабатывала от одного клика.

**Запрещено теперь.** Возвращать в UI горизонты 30/60/90 и поднимать `le` в
forecast-роутерах, пока в `forecasts` не появится хранение горизонта отдельной колонкой.

---

## 2026-08-09 — Инцидент: волна разведки положила прод-VM

**Что произошло.** Шесть параллельных агентов разведки дёргали `/api/forecast/*`.
Каждый такой GET синхронно обучает Prophet + NeuralProphet + XGBoost. VM: 3.8 GiB RAM,
**swap 0**, свободно было 971 MiB. Хост перестал отвечать (TCP-порты открыты,
SSH-баннер и HTTP не приходят) — картина OOM/трэшинга. Легли все проекты на VM:
tourism, hydra, ads-monitor, cadastral, leadagent, proxy-nginx. Восстановлено
перезагрузкой из консоли cloud.ru, данные не пострадали (289 отелей / 373 события целы).

**Почему это важно на будущее.** Тяжесть эндпоинта не следует из его HTTP-метода.
Запрет на мутации не делает волну безопасной для прод-хоста.

**Запрещено теперь.** Пускать параллельную волну агентов на эндпоинты, запускающие
обучение ML, без явного ограничения параллелизма и без swap на хосте. Обучение
из request-path подлежит выносу.

---

## 2026-08-09 — Эмбеддинги переводятся с GigaChat на локальные

**Решение.** ChromaDB переводится на локальную embedding-функцию
(`DefaultEmbeddingFunction`, all-MiniLM-L6-v2) вместо `GigaChatEmbeddings`.

**Почему.** RAG не проиндексировал ни одного документа за всё время жизни контейнера:
402 Payment Required с 06.06.2026 (выбита квота), затем 401 credentials с 29.06.
Корень не в ключе, а в том, что `reindex_chroma` переэмбеддит весь корпус (~662
документа) заново после каждого прогона парсеров — до 6+ полных прогонов в сутки.
Любой платный внешний эмбеддер на таком режиме выбивается по квоте независимо от
свежести ключа.

**Запрещено теперь.** Чинить RAG заменой ключа GigaChat без устранения полной
переиндексации — это лечение симптома, оно уже отказало дважды.
