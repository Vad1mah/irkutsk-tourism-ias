# Следы ИИ и внешние ссылки в ВКР и ТЭО

Аудит трёх файлов на упоминания методичек преподавателя, имена преподавателей в основном тексте, пути к файлам проекта, ссылки на git/GitHub, английские артефакты, B2B-rebuild/Phase-маркеры, конфиги (.env/docker-compose) и ссылки на несуществующие приложения.

Замечание по титульному листу: имя «Ю.В. Пестова» на строке 15 файла `OTCHET_PO_PRAKTIKE.md` — это поле титульного листа («Руководитель: ст. преподаватель»), оно ДОПУСТИМО и в аудит не включено.

---

## OTCHET_PO_PRAKTIKE.md

- Строка 97: «*[Здесь будет вставлен рисунок 11 – IDEF0 контекстная диаграмма (A-0): «Работа с данными туризма Прибайкалья». Источник файла: `models/IDEF0/A-0-A-0 Контекстная диаграмма.drawio.png`]*». Тип: 3 (путь файла проекта). Замена: убрать «Источник файла: …», оставить только «*[Рисунок 11 – IDEF0 контекстная диаграмма (A-0)]*». Аналогичные служебные комментарии — это чисто верстальная разметка для вставки изображений и они не должны попасть в финальный текст.

- Строка 323: «*[Здесь будет вставлен рисунок 12 – Use Case диаграмма (общая, 12 UC × 5 актёров). Источник файла: `models/UC/Общая диаграмма UC.drawio.png`]*». Тип: 3. Замена: «*[Рисунок 12 — общая Use Case диаграмма системы]*» (либо удалить плейсхолдер, оставив только подпись «Рисунок 12 – …»).

- Строка 327: «Полное описание сценариев каждого UC – пред- и постусловия, основной и альтернативные потоки, отношения include/extend – вынесено в `models/UC/UC_SCENARIOS.md` и в Приложение Б к ВКР.». Тип: 3 (путь к файлу проекта). Замена: «Полное описание сценариев каждого UC … вынесено в Приложение Б к настоящей работе.» (упоминание `models/UC/UC_SCENARIOS.md` удалить).

- Строка 396: «*[Здесь будет вставлен рисунок 13 – IDEF0 декомпозиция первого уровня (A0): пять подфункций «Работы с данными туризма». Источник файла: `models/IDEF0/A-0-A0 Первый уровень.drawio.png`]*». Тип: 3. Замена: убрать «Источник файла: …».

- Строка 432: «*[Здесь будет вставлен рисунок 14 – Диаграмма предметной области (Domain Model). Источник файла: `models/DOMAIN/Диаграмма предметной области.drawio.png`]*». Тип: 3. Замена: убрать путь.

- Строка 456: «*[Здесь будет вставлен рисунок 15 – Диаграмма компонентов (Component Diagram). Источник файла: `models/COMPONENT/component_diagrams.drawio`]*». Тип: 3, 5 (артефакт на латинице). Замена: убрать путь.

- Строка 460: «*[Здесь будет вставлен рисунок 16 – Диаграмма развёртывания (Deployment Diagram, Docker Compose). Источник файла: `models/COMPONENT/deployment_diagrams.drawio`]*». Тип: 3, 5. Замена: убрать путь.

- Строка 466: «*[Здесь будет вставлен рисунок 17 – Диаграммы бизнес-процессов в нотации Erikson-Penker. Источник файлов: `models/BP/Контекстная диаграмма.drawio.png`, `models/BP/Декомпозиция.drawio.png`]*». Тип: 3. Замена: убрать пути.

- Строка 496: «*[Здесь будет вставлен рисунок 18 – Диаграмма последовательности UC1 «Получить прогноз загрузки» (Sequence Diagram). Источник файла: `models/SEQUENCE/диаграмма последовательности UC1 Построить прогноз.drawio.png`]*». Тип: 3. Замена: убрать путь.

- Строка 512: «…RevPAR/ADR/Occupancy района через прокси-цены 101Hotels)…», далее: «…(MethodologyService)… (ParserHealthService)…». Тип: 6 (внутренние имена сервисов из инженерной кодовой базы — приемлемо в техническом тексте, но рядом с ними в строке 514 есть более чёткая инженерная деталь — `Redis-хеше с TTL 7 дней`, которая допустима). Замена: оставить как есть, имена `MethodologyService`/`ParserHealthService` корректно описаны как «сервис методологической прозрачности» и «сервис здоровья парсеров».

- Строка 516: «*[Здесь будет вставлен рисунок 19 – Диаграмма последовательности UC11 «Анализ событий с corrected impact» (Sequence Diagram). Источник файла: `models/SEQUENCE/uc11_sequence.drawio`]*». Тип: 3, 5. Замена: убрать «Источник файла: `models/SEQUENCE/uc11_sequence.drawio`».

- Строка 618: «Модульные тесты – **205 функций в 34 файлах** в каталоге `backend/tests/test_*.py`. Они покрывают: инструменты AI-агента (test_agent_tools, test_agent_new_tools), ансамблевый сервис и кэширование (test_ensemble_service, test_ensemble_cache_key), … `tests/e2e_test.py` … `tests/agent_stress_test.py` … `tests/test_persona_walkthrough.py` …». Тип: 3 (пути и имена файлов проекта на латинице), 5. Замена: переписать в нейтральном виде: «Модульные тесты — 205 функций в 34 файлах — покрывают инструменты AI-агента, ансамблевый сервис и кэширование, помощники прогнозирования, температурные настройки LLM, валидацию метрик, API-роутеры, Pydantic-схемы, безопасность, новые B2B-эндпоинты, парсеры, здоровье парсеров и стрим-счётчик SSE. Сквозные (E2E) тесты включают 9 сценариев: health-endpoint, KPI, список отелей, события, прогноз Prophet, ансамблевый прогноз, погода, запрос к AI-агенту, статус планировщика. Стресс-тестирование AI-агента — 19 + 6 сценариев с rubric-оценкой 0–5 баллов; финальный прогон 8 мая 2026 года — 19/19 успешных ответов при средней оценке 4,63/5. Дополнительный пользовательский сценарий обходит интерфейс глазами трёх профильных сегментов (отельер, администрация, исследователь).» (удалены `backend/tests/test_*.py`, `tests/e2e_test.py`, `tests/agent_stress_test.py`, `tests/test_persona_walkthrough.py` и список английских имён test_*).

- Строка 622: «…выполнено восстановление мартовских пробелов 2026 года (02–12 марта и 17–26 марта) через скрипт `scripts/import_from_ydb_101hotels.py --from ... --to ...`, что добавило 3 287 строк статистики…». Тип: 3 (путь к скрипту). Замена: «…выполнено восстановление мартовских пробелов 2026 года (02–12 и 17–26 марта) через служебный скрипт миграции данных из YDB, что добавило 3 287 строк статистики…».

- Строка 626: «…Преподавательская методичка рекомендует приводить оба варианта…» (по контексту 2.4 ТЭО — упоминание «методички преподавателя» в ВКР). Тип: 1, 10. Замена: удалить отсылку, написать «В инвестиционном анализе принято приводить оба варианта: первый показывает академическую корректность учёта трудоёмкости, второй — реальную инвестиционную привлекательность.»

- Строка 690: «Скрипт `scripts/monte_carlo_teo.py` детерминированно воспроизводит результат при seed=42; параметры распределений и формулы расчёта изложены в разделе 8 Приложения А.». Тип: 3 (путь к скрипту). Замена: «Имитационная модель реализована программно (Python, NumPy) и детерминированно воспроизводит результат при фиксированном seed; параметры распределений и формулы расчёта изложены в разделе 8 Приложения А.»

- Строка 733: «B2B-рефокус завершён в ходе преддипломной практики (06.04–04.05.2026) в пяти фазах (~52 задачи, ~69 коммитов): бэкенд-схема и дедупликация (фаза 1), расширение AI-агента до 12 инструментов (фаза 2), перестройка фронтенда (фаза 3), UML/BPMN-модели (фаза 4), верификация (фаза 5); сводка зафиксирована в [`docs/PHASE_REBUILD_SUMMARY.md`](../PHASE_REBUILD_SUMMARY.md).». Тип: 3, 6 (внутренние «Phase 1…5», «PHASE_REBUILD_SUMMARY.md», счётчик коммитов). Замена: «Работы по преддипломной практике (06.04–04.05.2026) включали пять последовательных этапов: расширение бэкенд-схемы и дедупликация, расширение AI-агента до двенадцати инструментов, перестройка фронтенда под B2B-сценарии, разработка UML/BPMN-моделей, итоговая верификация. Промежуточные итоги фиксировались в журнале работ.» (без счётчика коммитов и без ссылки на внутренний `docs/PHASE_REBUILD_SUMMARY.md`).

- Строка 750: «5. Yandex DataLens [Электронный ресурс] : документация…». Тип: — (внешний URL `cloud.yandex.ru/docs/` — допустимо: это библиографическая ссылка на публичную документацию). Действий не требуется.

- Строка 758: «9. MagicStack/asyncpg [Электронный ресурс] : асинхронный драйвер PostgreSQL для Python. – URL: https://github.com/MagicStack/asyncpg …». Тип: 4 (ссылка на github). Замена: оставить ссылку (это публичная страница официального проекта-библиотеки в библиографии — допустимо), либо заменить на ссылку с домена pypi.org/project/asyncpg/. По формальному критерию задания (запрет на ссылки github) корректнее заменить на `https://magicstack.github.io/asyncpg/current/` или `https://pypi.org/project/asyncpg/`.

- Строка 786: «23. LangGraph [Электронный ресурс] : документация фреймворка. – URL: https://langchain-ai.github.io/langgraph/». Тип: 4 (домен `langchain-ai.github.io`). Замена: оставить — это доменное имя официальной документации фреймворка, не ссылка на репозиторий проекта-ВКР; либо заменить на `https://langchain-ai.com/langgraph/` если есть mirror. По формальному критерию запрета на github лучше заменить на ссылку с собственного домена langchain-ai.

- Строка 818: «39. PostgreSQL 16 Documentation [Электронный ресурс]. – URL: https://www.postgresql.org/docs/16/». Тип: — (публичная документация СУБД, допустимо).

---

## TEO_POLNOE.md

(Файл в `docs/project/` является источником для Приложения А — но не входит в комплект защиты под этим именем; в аудит включены только проблемные фрагменты, которые попадут в Приложение А, см. ниже.)

- Строка 13: «Шаблон: методические материалы преподавателя по дисциплине ТЭО, версия 12.05.2026.». Тип: 1. Замена: «Документ подготовлен по типовой структуре технико-экономического обоснования инвестиционного проекта.»

- Строка 175: «Система упакована в Docker. `docker-compose.yml` определяет четыре сервиса… секреты передаются через `.env`.». Тип: 7 (имена конфигов). Замена: «Система упакована в Docker и развёртывается единой связкой из четырёх сервисов (PostgreSQL 16 в alpine-сборке с healthcheck, Redis 7 alpine с аутентификацией, backend FastAPI, frontend на Nginx). Образы собираются по multi-stage Dockerfile … Контейнеры запускаются под non-root пользователем, секреты передаются через переменные окружения.»

- Строка 200: «| Контейнеризация | Docker + docker-compose | 25.0 | …». Тип: 7. Замена: «Docker (compose-сборка) | 25.0 |…» — заменить «docker-compose» на «compose-сборка».

- Строка 209: «Научный руководитель — Ю. В. Пестова — выполняет роль постановщика задач и приёмщика результатов…». Тип: 2 (имя руководителя в основном тексте). Замена: «Научный руководитель проекта выполняет роль постановщика задач и приёмщика результатов…» (по тексту работы — без фамилии).

- Строка 219: «| Пестова Ю. В. | Научный руководитель | Постановка задач, проверка результатов, утверждение требований, защита перед комиссией |». Тип: 2. Замена: «Научный руководитель | Постановка задач, проверка результатов, утверждение требований, согласование защиты |».

- Строка 224: «Календарный план зафиксирован в индивидуальном задании от 6 апреля 2026 года и охватывает 36 рабочих дней… План-график приведён в таблице 5 и совпадает с этапами дорожной карты B2B-рефокуса.». Тип: 6. Замена: «План-график приведён в таблице 5 и соответствует дорожной карте развития системы.» (без слова «B2B-рефокус» — это внутренний инженерный термин).

- Строка 409: «Расчёт экономической эффективности выполнен по методологии, изложенной в материалах преподавателя `new_tasks/9-otsenka-ekonomicheskoy-effektivnosti-proekta.pdf` и `new_tasks/10-otsenka-ekonomicheskoy-effektivnosti-proekta.pdf`. Использованы три стандартных показателя инвестиционного анализа: NPV, PP, DPP. Дополнительно … имитационная модель Монте-Карло, реализованная в `scripts/monte_carlo_teo.py`; подробные результаты приведены в подразделе 8.6 и в Приложении М к ТЭО.». Тип: 1, 3, 8, 9 (методичка, пути, английские имена файлов, ссылка на «Приложение М» которого в защите нет). Замена: «Расчёт экономической эффективности выполнен по стандартной методологии оценки инвестиционных проектов. Использованы три классических показателя: чистая приведённая стоимость (NPV), простой срок окупаемости (PP) и дисконтированный срок окупаемости (DPP). Дополнительно рассчитан Profitability Index (PI) и проведены два вида анализа риска — сценарный и стохастический (имитационная модель Монте-Карло). Подробные результаты приведены в подразделе 8.6.» (упоминания `new_tasks/...pdf`, `scripts/monte_carlo_teo.py` и Приложения М удалены).

- Строка 417: «Преподавательская методичка рекомендует приводить оба варианта…». Тип: 1, 10. Замена: «В инвестиционном анализе принято приводить оба варианта: первый — для академической корректности учёта трудоёмкости, второй — для оценки реальной инвестиционной привлекательности.»

- Строка 429: «Расчёт выполнен помесячно по детерминистической модели (penetration=1, churn=0, hosting=4 500 ₽), реализованной в `scripts/monte_carlo_teo.py --deterministic`. … полная помесячная таблица сохранена в `docs/project/teo_baseline_monthly.csv`.». Тип: 3, 5. Замена: «Расчёт выполнен помесячно по детерминистической модели (penetration = 1, churn = 0, hosting = 4 500 ₽). Ниже приведён сводный квартальный срез; полная помесячная таблица вынесена в Приложение А.»

- Строка 467: «Простой срок окупаемости рассчитан как момент перехода кумулятивного денежного потока в положительную область … Значения совпадают с расчётом скрипта `monte_carlo_teo.py --deterministic`.». Тип: 3, 5. Замена: «… Значения проверены численно по той же помесячной модели.»

- Строка 499: «…методический образец которой содержится в файле `new_tasks/monte-karlo-ZAPOLNENO.xlsm`. В разработанном проекте Монте-Карло реализован Python-скриптом `scripts/monte_carlo_teo.py`, выполняющим 10 000 случайных симуляций…». Тип: 1, 3, 5, 8. Замена: «… Имитационная модель Монте-Карло реализована программно на Python с использованием NumPy и выполняет 10 000 случайных симуляций со следующими стохастическими параметрами:».

- Строка 503: «…необходимость перехода на B2.large при росте нагрузки.». Тип: — (тариф облака допустим).

- Строка 505: «…воспроизводимо командой `python scripts/monte_carlo_teo.py --n 10000 --seed 42`…». Тип: 3. Замена: «…воспроизводимо при фиксированном seed = 42 с числом симуляций N = 10 000…».

- Строка 518: «Гистограмма распределения NPV (полный) приведена в Приложении М к ТЭО.». Тип: 9 (Приложение М в составе ВКР отсутствует). Замена: «Гистограмма распределения NPV (полный вариант) приведена на рисунке 4 настоящего раздела.» (или удалить отсылку, если рисунок не вставляется).

- Строка 582: «*Руководитель: Пестова Ю. В.*». Тип: 2 (имя в основном тексте — для полного ТЭО). Замена: для версии, выложенной как Приложение А, можно оставить только в шапке/титуле; либо удалить эту строку из подвала.

---

## Приложение_А_ТЭО.md

(Файл идентичен `TEO_POLNOE.md` по структуре и содержит те же проблемные фрагменты на тех же строках. Дополнительно — собственный заголовок «ПРИЛОЖЕНИЕ А».)

- Строка 9: «Руководитель: Пестова Юлия Владимировна». Тип: 2 (если не считать «титульной» частью Приложения). Замена: для приложения такой подпись допустима в шапке/титуле, оставить как есть. Если фамилия должна оставаться только на титульном листе ВКР — удалить и заменить на нейтральную форму без имени.

- Строка 13: «Шаблон: методические материалы преподавателя по дисциплине ТЭО (Осипов М. А.), задания 2–8 с примерами `zadanie-*-VYPOLNENO.docx`, образец имитационной модели Монте-Карло `monte-karlo-ZAPOLNENO.xlsm`.». Тип: 1, 2, 5, 8. Замена: ВЕСЬ блок «Шаблон: …» удалить. Структура работы и так понятна из содержания; ссылок на внешние материалы преподавателя в готовом приложении быть не должно.

- Строка 200: «| Контейнеризация | Docker + docker-compose | 25.0 | Multi-stage, healthchecks |». Тип: 7. Замена: «Docker (compose-сборка) | 25.0 | Multi-stage, healthchecks» (убрать `docker-compose`).

- Строка 209: «…Научный руководитель — Ю. В. Пестова — выполняет роль постановщика задач…». Тип: 2. Замена: «Научный руководитель проекта выполняет роль постановщика задач и приёмщика результатов…»

- Строка 219: «| Пестова Ю. В. | Научный руководитель |…». Тип: 2. Замена: «Научный руководитель |…».

- Строка 224: «… совпадает с этапами дорожной карты B2B-рефокуса.». Тип: 6. Замена: «… соответствует дорожной карте развития системы.»

- Строка 409: см. TEO_POLNOE.md строка 409 (упоминания `new_tasks/...pdf`, `scripts/monte_carlo_teo.py`, «Приложение М»). Тип: 1, 3, 8, 9. Замена: см. выше для TEO_POLNOE.md (строка 409).

- Строка 417: «Преподавательская методичка рекомендует приводить оба варианта…». Тип: 1, 10. Замена: см. выше.

- Строка 429: «… `scripts/monte_carlo_teo.py --deterministic` … `docs/project/teo_baseline_monthly.csv`». Тип: 3, 5. Замена: см. выше для TEO_POLNOE.md.

- Строка 467: «… `monte_carlo_teo.py --deterministic`». Тип: 3, 5. Замена: убрать имя скрипта; «…значения проверены численно по той же помесячной модели.»

- Строка 499: «…методический образец которой содержится в файле `new_tasks/monte-karlo-ZAPOLNENO.xlsm`. В разработанном проекте Монте-Карло реализован Python-скриптом `scripts/monte_carlo_teo.py`…». Тип: 1, 3, 5, 8. Замена: «Имитационная модель Монте-Карло реализована программно на Python с использованием NumPy и выполняет 10 000 случайных симуляций со следующими стохастическими параметрами:».

- Строка 505: «…воспроизводимо командой `python scripts/monte_carlo_teo.py --n 10000 --seed 42`…». Тип: 3. Замена: «…воспроизводимо при фиксированном seed = 42 с числом симуляций N = 10 000…».

- Строка 518: «Гистограмма распределения NPV (полный) приведена в Приложении М к ТЭО.». Тип: 9. Замена: либо «Гистограмма распределения NPV (полный вариант) приведена на рисунке 4 раздела 8 настоящего Приложения», либо удалить отсылку.

- Строка 582: «*Руководитель: Пестова Ю. В.*». Тип: 2. Замена: удалить из подвала (имя руководителя уже стоит в шапке Приложения).

---

## Сводка типов нарушений

| Тип | Краткое описание | Кол-во находок |
|-----|------------------|----------------|
| 1 | Методички преподавателя | 6 (стр. 13, 409, 417, 499 в обоих ТЭО-файлах + стр. 626 в OTCHET) |
| 2 | Имена преподавателей в основном тексте | 4 (стр. 9, 209, 219, 582 в обоих ТЭО-файлах) |
| 3 | Пути к файлам проекта (`scripts/`, `models/`, `docs/`, `backend/`) | 17+ (рисунки 11–19 + stack-описания + tests-описание) |
| 4 | Ссылки на github.com / *.github.io | 2 (стр. 758, 786 OTCHET) |
| 5 | Английские артефакты (`.xlsm`, `.docx`, `.csv`, имена `test_*`, `*.drawio`) | 8+ |
| 6 | B2B-rebuild / Phase 1–6 | 2 (стр. 733 OTCHET, стр. 224 ТЭО) |
| 7 | `.env`, `docker-compose.yml` | 2 (стр. 175, 200 в обоих ТЭО) |
| 8 | Английские названия методичек на латинице | 3 (стр. 13, 409, 499 ТЭО — `zadanie-*-VYPOLNENO.docx`, `monte-karlo-ZAPOLNENO.xlsm`, `new_tasks/*.pdf`) |
| 9 | Ссылки на отсутствующее «Приложение М» | 3 (стр. 409, 518 в обоих ТЭО-файлах) |
| 10 | «По методичке/образцу преподавателя» | 2 (стр. 417 ТЭО, стр. 626 OTCHET) |

## Рекомендованный порядок правок

1. **Заменить на нейтральные формулировки** все упоминания «методички преподавателя», «преподавательская методичка», «методический образец из new_tasks» — это самые громкие маркеры (тип 1, 10).
2. **Удалить пути и имена файлов** в плейсхолдерах рисунков OTCHET — оставить только подписи «Рисунок N — …» (тип 3, 5).
3. **Удалить упоминания Приложения М** из обоих ТЭО — этого приложения в комплекте защиты нет (тип 9).
4. **Имена руководителя** оставить только на титульных листах (ВКР, индивидуальное задание, Приложение А-шапка); из основного текста разделов 5 и подвалов удалить (тип 2).
5. **Заменить пути к скриптам** (`scripts/monte_carlo_teo.py`, `scripts/import_from_ydb_101hotels.py`) на нейтральное «программная реализация модели на Python» (тип 3).
6. **Убрать жаргон B2B-rebuild / Phase 1–5 / счётчик коммитов** из стр. 733 OTCHET и стр. 224 ТЭО — заменить на «этапы развития системы» / «работы по преддипломной практике в пять последовательных этапов» (тип 6).
7. **Убрать `.env`, `docker-compose.yml`** из стр. 175 и 200 ТЭО — заменить на «переменные окружения» и «Docker (compose-сборка)» (тип 7).
8. **Заменить github-ссылки** в библиографии на ссылки с собственных доменов библиотек (`magicstack.github.io/asyncpg/`, `pypi.org/project/asyncpg/`, и т. п.) для строгого соответствия требованию (тип 4).

Все указанные правки можно выполнить точечной заменой — общая картина текста, расчёты и аргументация при этом не страдают.
