# Диаграмма предметной области (Domain Model)

## Назначение

Диаграмма предметной области описывает основные понятия (объекты) предметной области ИАС мониторинга и прогнозирования туристической активности Иркутской области и отношения между ними. Объекты выявлены на основе анализа сценариев вариантов использования (UC1–UC10) **под B2B-концепцию системы**: три профильных сегмента — отельеры, региональная администрация, исследователи.

## Объекты предметной области

| Объект | Описание | Источник (UC) |
|--------|----------|---------------|
| Отельер | Владелец / менеджер средства размещения. Использует прогноз и RMS-метрики для управления доходностью своего объекта | UC1, UC2, UC10 |
| Администратор региона | Сотрудник Министерства туризма / Агентства по туризму. Использует региональную аналитику для управленческих решений | UC1, UC3, UC5 |
| Исследователь | Профильный экономист / urban-аналитик. Использует методологию и экспорт данных | UC3, UC4, UC9 |
| Технический администратор | ИТ-специалист, обслуживающий систему. Управляет источниками и мониторит здоровье | UC6, UC7 |
| B2B-кабинет | Логическая агрегация прав и контекста B2B-пользователя (отельер / администратор / исследователь). Виртуальная сущность для будущего разделения доступа | UC1, UC2, UC9, UC10 |
| Запрос | Текстовый или параметрический запрос пользователя к системе | UC1, UC3, UC9 |
| Ответ | Структурированный ответ системы (JSON / CSV / визуализация) | UC1–UC5, UC10 |
| AI-агент (LangGraph) | MainAgent + ForecastAgent на базе LangGraph (StateGraph, Command pattern, MemorySaver). 12 tools: 6 базовых (search_hotels, search_events, get_weather, forecast_occupancy, get_statistics, get_revenue_metrics) + 6 RMS-расширений (get_top_events_by_impact, get_booking_pace, compare_districts, compare_forecast_models, get_occupancy_timeseries, get_price_distribution). LLM-fallback chain Groq → DeepSeek → Mistral в `main_agent.call_model` | UC1, UC3 |
| Прогноз (Ensemble) | Ансамблевое предсказание загрузки: Prophet + NeuralProphet + XGBoost → weighted average. Async через executor.run_sync | UC1, UC4, UC10 |
| Модель прогнозирования | Prophet / NeuralProphet / XGBoost — индивидуальная модель Ensemble | UC1, UC4 |
| Метрики модели | RMSE, MAE, R² — оценка качества модели на тестовых данных | UC4 |
| Feature Engineering | 38 признаков (календарь, праздники, лаги, скользящие средние, погодные, событийные, тренд, цены) | UC1 |
| Погодный прогноз | Данные Open-Meteo API: температура, осадки, ветер | UC1, UC8 |
| Средство размещения | Гостиница, санаторий, гостевой дом, хостел и др. (~370 КСР по реестру Иркутской области + 179 санаториев; 1 428 объектов в БД на 7 мая 2026 года; 18 типов размещения) | UC2, UC5, UC8 |
| Тип размещения | Классификация средств размещения (отель / санаторий / хостел / гостевой дом) | UC2, UC5 |
| Район | Территориальная принадлежность объекта (15 районов Иркутской области) | UC2, UC5 |
| Номерной фонд | Комнаты и номера средства размещения | UC2 |
| Статистика загруженности (HotelStatistic) | Данные о занятости номеров по датам: rooms_num, free_rooms_amount, available_rooms_percent, min_price | UC1, UC2, UC10 |
| RMS-метрика | Метрика Revenue Management: RevPAR, ADR, Occupancy %, Pickup, Pace. Рассчитывается из HotelStatistic | UC10 |
| Heatmap загрузки | Матрица 7×12 (дни недели × месяцы) средней загрузки объекта/района | UC5, UC10 |
| Событие | Мероприятие региона (концерт, фестиваль, выставка, спорт, конференция). 8 источников парсинга | UC3, UC8 |
| Тип события | Классификация событий | UC3 |
| Impact события | Расчётное влияние события на загрузку: Δ Occupancy = Occupancy(день события) − среднее ±7 дней | UC3 |
| Экспорт данных | CSV-выгрузка по запросу через `/api/analytics/export?type=occupancy\|events\|hotels` | UC9 |
| Источник данных | Внешний ресурс для сбора информации (101Hotels, Xotelo, 8 парсеров событий, Open-Meteo, Telegram) | UC6, UC8 |
| Лог системы | Журнал работы (Python logging + HealthMonitor) | UC7 |
| Health-check | Проверка состояния сервиса (PostgreSQL, Redis, ChromaDB, ML, LLM) | UC7 |
| Планировщик (APScheduler) | Автоматический запуск задач по расписанию (события 6ч, отели 2ч, погода 3ч, Telegram 1ч) | UC8 |
| Векторный индекс (ChromaDB) | Эмбеддинги документов для RAG-поиска (GigaChat Embeddings, ~1 075 документов) | UC1, UC3 |
| Кэш (Redis) | Кэширование прогнозов и API-ответов (TTL 30 мин) | UC1, UC10 |
| BookingSnapshot | Снимок occupancy на момент времени для будущей даты заезда. Поля: `snapshot_taken_at` (datetime), `target_date` (date), `district` (string), `occupancy_pct` (float). Используется для proxy-pickup (FR3.9) | UC10 |
| EventImpactRecord | Рассчитанная impact-запись для события. Поля: `event_id` (FK Event), `district` (string), `baseline_occupancy_mean` (float), `observed_occupancy` (float), `delta_pct` (float), `ci_lower` / `ci_upper` (float), `n_samples` (int), `confidence` (enum: high/medium/low), `method` (string: 'seasonal_corrected' \| 'naive'), `computed_at` (datetime) | UC11 |
| SegmentBenchmark | Агрегированные метрики по сегменту (район × размерная категория). Поля: `district` (string), `size_bucket` (enum: mini/mid/large), `n_in_segment` (int), `avg_occupancy` (float), `avg_min_price` (int), `computed_at` (datetime). Категории: mini ≤ 15 номеров / mid 16–50 / large 51+ | UC12 |
| ParserHealthRecord | Redis-stored запись о статусе парсера. Поля: `parser_id` (string), `status` (enum: ok/warn/fail), `items_collected` (int), `error` (string nullable), `last_run` (datetime). Используется для `GET /api/parser/health`, FR-monitoring | UC7 |
| MethodologyRecord | Методологическая запись для прозрачности. Поля: `metric_name` (string), `formula_text` (string), `version` (string), `valid_from` (datetime), `source_endpoint` (string). Связь с NFR7 «методологическая прозрачность» | UC4, UC9 |

## Отношения

### Ассоциации

| Объект 1 | Отношение | Объект 2 |
|----------|-----------|----------|
| Отельер | использует | B2B-кабинет |
| Администратор региона | использует | B2B-кабинет |
| Исследователь | использует | B2B-кабинет |
| B2B-кабинет | формирует | Запрос |
| AI-агент (LangGraph) | обрабатывает | Запрос |
| AI-агент (LangGraph) | формирует | Ответ |
| AI-агент (LangGraph) | вызывает | Прогноз (Ensemble) |
| AI-агент (LangGraph) | вызывает | RMS-метрика |
| Прогноз (Ensemble) | использует | Модель прогнозирования |
| Прогноз (Ensemble) | использует | Feature Engineering |
| Прогноз (Ensemble) | учитывает | Погодный прогноз |
| Прогноз (Ensemble) | учитывает | Событие |
| Прогноз (Ensemble) | строится на основе | Статистика загруженности |
| Прогноз (Ensemble) | связан с | Средство размещения |
| Прогноз (Ensemble) | имеет | Метрики модели |
| Запрос | возвращает | Ответ |
| Средство размещения | имеет | Тип размещения |
| Средство размещения | расположено в | Район |
| Средство размещения | имеет | Номерной фонд |
| Событие | имеет | Тип события |
| Событие | имеет | Impact события |
| Impact события | рассчитан на основе | Статистика загруженности |
| RMS-метрика | рассчитана для | Средство размещения |
| RMS-метрика | агрегирована по | Район |
| Heatmap загрузки | агрегирует | Статистика загруженности |
| Отельер | владеет | Средство размещения |
| Администратор региона | мониторит | Район |
| Исследователь | запрашивает | Экспорт данных |
| Экспорт данных | формируется из | Статистика загруженности |
| Экспорт данных | формируется из | Событие |
| Экспорт данных | формируется из | Средство размещения |
| Технический администратор | управляет | Источник данных |
| Технический администратор | просматривает | Лог системы |
| Технический администратор | просматривает | Health-check |
| Планировщик (APScheduler) | запускает сбор | Источник данных |
| Источник данных | предоставляет | Средство размещения |
| Источник данных | предоставляет | Событие |
| Источник данных | предоставляет | Погодный прогноз |
| Векторный индекс (ChromaDB) | используется для | AI-агент (LangGraph) |
| Кэш (Redis) | хранит | Прогноз (Ensemble) |
| Кэш (Redis) | хранит | RMS-метрика |
| EventImpactRecord | связан с | Событие |
| EventImpactRecord | рассчитан через | MethodologyRecord |
| SegmentBenchmark | агрегирует | Статистика загруженности |
| SegmentBenchmark | фильтруется по | Район |
| BookingSnapshot | фиксирует | Статистика загруженности |
| ParserHealthRecord | описывает | Источник данных |
| Кэш (Redis) | хранит | ParserHealthRecord |

### Агрегации (часть-целое)

| Целое | содержит | Часть |
|-------|----------|-------|
| Средство размещения | содержит | Номерной фонд |
| Средство размещения | содержит | Статистика загруженности |
| Статистика загруженности | агрегируется в | RMS-метрика |
| Статистика загруженности | агрегируется в | Heatmap загрузки |
| Район | содержит | Средство размещения |
| Календарь событий | содержит | Событие |
| Прогноз (Ensemble) | содержит | Модель прогнозирования (3 шт.) |

### Обобщения (наследование)

| Потомок | является | Предок |
|---------|----------|--------|
| Отельер | является | B2B-пользователь (роль) |
| Администратор региона | является | B2B-пользователь (роль) |
| Исследователь | является | B2B-пользователь (роль) |

## Связь с другими моделями

| Модель | Связь |
|--------|-------|
| Use Case | Объекты выявлены из сценариев UC1–UC10 |
| IDEF1X | Объекты соответствуют сущностям БД (Hotel, HotelStatistic, Event и др.) |
| Requirements | Объекты реализуют функциональные требования FR1–FR6 |
| IDEF3 | Объекты участвуют в процессах сбора и обработки данных |
| COMPONENT | Объекты обслуживаются компонентами B2B-API, ML-сервисами и сервисом экспорта |

## Файлы

- `domain_model.drawio` - диаграмма предметной области
